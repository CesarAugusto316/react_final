{
  // Place your snippets for typescriptreact here. Each snippet is defined under a snippet name and has a prefix, body and
  // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
  // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the
  // same ids are connected.
  // Example:
  // "Print to console": {
  // 	"prefix": "log",
  // 	"body": [
  // 		"console.log('$1');",
  // 		"$2"
  // 	],
  // 	"description": "Log output to console"
  // }
  "file read from input": {
    "prefix": "rfileread",
    "body": [
      "const [imageUrl, setImageUrl] = useState('');",
      "",
      "const readFile = (file: File) => {",
      "  if (file.type && !file.type.startsWith('image/')) {",
      "    return;",
      "  }",
      "  const reader = new FileReader();",
      "  new Promise<string>((resolve) => {",
      "    reader.addEventListener('load', (e) => {",
      "      const result = e.target?.result as string;",
      "      resolve(result);",
      "    });",
      "  }).then((src) => {",
      "    setImageUrl(src); // Hook to update ImageURL",
      "  });",
      "",
      "  reader.readAsDataURL(file); // this triggers the event handler",
      "};",
      "",
      "// <div className='image-uploaded'>",
      "   // {imageUrl.length",
      "   // ? <img src={imageUrl} className=\"form__image\" />",
      "   // : null}",
      "// </div>",
    ],
    "description": "function to read a file from input such an image"
  },
  "import redux store, Provider": {
    "prefix": "rximpstoProv",
    "body": [
      "import { Provider } from 'react-redux';",
      "import { store } from './redux/store';",
      "",
      "",
      "<Provider store={store}>",
      "</Provider>"
    ],
    "description": "imports redux store and Provider Component"
  },
  "React Functional Component": {
    "prefix": "rfc",
    "body": [
      "import { FC } from 'react';",
      "",
      "",
      "export const ${1:${TM_FILENAME_BASE}}: FC = () => {",
      "  return <div>",
      "    $2",
      "  </div>;",
      "};"
    ],
    "description": "Creates a React Functional Component for Typescript"
  },
  "import redux Hooks": {
    "prefix": "rximpuse",
    "body": [
      "import { useTypedSelector, useTypedDispatch } from './redux/store';"
    ],
    "description": "import redux Typed Hooks in React Components"
  }
}